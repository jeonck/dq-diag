# 점수 계산 개선 사항 (Scoring Improvements)

## 📋 개요

사용자 피드백에 따라 점수 계산 알고리즘을 **훨씬 더 엄격하게** 조정했습니다.

**문제점**: 많은 오류가 있는 데이터(NULL 50%, 중복 30%, 미래 날짜, 음수 등)가 95% 이상의 점수를 받음

**목표**: 실제 데이터 품질 문제를 정확히 반영하는 점수 제공 (30-50% 범위)

---

## 🔧 모듈별 개선 내용

### 1. **완전성 (Completeness)** - `completeness.py`

#### 변경 전:
```python
base_score = completeness_rate * 0.7  # 70% 가중치
issue_penalty = min(issue_count * 5, 30)  # 이슈당 5점
issue_score = 30 - issue_penalty
```

#### 변경 후:
```python
base_score = completeness_rate * 0.6  # 60% 가중치
issue_penalty = min(issue_count * 8, 40)  # 이슈당 8점
issue_score = 40 - issue_penalty

# 추가 감점
if completeness_rate < 50:
    total_score *= 0.5  # 완전성 50% 미만 시 절반
elif completeness_rate < 70:
    total_score *= 0.7  # 완전성 70% 미만 시 30% 감점
```

**주요 변경점**:
- 기본 가중치 70% → 60%
- 이슈당 감점 5점 → 8점
- 이슈 최대 감점 30점 → 40점
- 완전성 <50%: 50% 추가 감점
- 완전성 <70%: 30% 추가 감점

---

### 2. **일관성 (Consistency)** - `consistency.py`

#### 변경 전:
```python
duplicate_score = max(0, 100 - duplicate_rate * 2) * 0.5
issue_penalty = min(issue_count * 5, 50)
```

#### 변경 후:
```python
duplicate_score = max(0, 100 - duplicate_rate * 5) * 0.5  # 2배 → 5배
issue_penalty = min(issue_count * 10, 50)  # 5점 → 10점

# 추가 감점
if duplicate_rate >= 20:
    total_score *= 0.5  # 중복률 20% 이상 시 절반
elif duplicate_rate >= 10:
    total_score *= 0.7  # 중복률 10% 이상 시 30% 감점
```

**주요 변경점**:
- 중복률 민감도 2배 → 5배
- 이슈당 감점 5점 → 10점
- 중복률 ≥20%: 50% 추가 감점
- 중복률 ≥10%: 30% 추가 감점

---

### 3. **정확성 (Accuracy)** - `accuracy.py`

#### 변경 전:
```python
base_score = accuracy_rate * 0.7  # 70% 가중치
issue_penalty = min(issue_count * 4, 30)  # 이슈당 4점
```

#### 변경 후:
```python
base_score = accuracy_rate * 0.6  # 60% 가중치
issue_penalty = min(issue_count * 10, 40)  # 이슈당 10점

# 추가 감점
if accuracy_rate < 50:
    total_score *= 0.5
elif accuracy_rate < 70:
    total_score *= 0.7
```

**추가 검사 항목**:
- ✅ 연도 범위 검사 추가 (1900 ~ 현재+1년)
- ✅ 미래 연도 감지 (2025-2030)
- ✅ 과거 연도 감지 (1899)

**주요 변경점**:
- 기본 가중치 70% → 60%
- 이슈당 감점 4점 → 10점
- 이슈 최대 감점 30점 → 40점
- 정확성 <50%: 50% 추가 감점
- 정확성 <70%: 30% 추가 감점

---

### 4. **보안성 (Security)** - `security.py`

#### 변경 전:
```python
sensitive_penalty = min(sensitive_count * 10, 50)
issue_penalty = min(issue_count * 5, 50)
```

#### 변경 후:
```python
sensitive_penalty = min(sensitive_count * 15, 60)  # 10점 → 15점
issue_penalty = min(issue_count * 8, 40)  # 5점 → 8점

# 추가 감점
if sensitive_count > 5:
    total_score *= 0.6  # 민감정보 6개 이상 시 40% 감점
elif sensitive_count > 3:
    total_score *= 0.8  # 민감정보 4개 이상 시 20% 감점
```

**주요 변경점**:
- 민감정보당 감점 10점 → 15점
- 이슈당 감점 5점 → 8점
- 민감정보 >5: 40% 추가 감점
- 민감정보 >3: 20% 추가 감점

---

### 5. **적시성 (Timeliness)** - `timeliness.py`

#### 변경 전:
```python
if days_old > 365: base_score -= 30
elif days_old > 180: base_score -= 20
elif days_old > 90: base_score -= 10
issue_penalty = min(len(issues) * 5, 30)
```

#### 변경 후:
```python
if days_old > 365: base_score -= 40  # 30점 → 40점
elif days_old > 180: base_score -= 25  # 20점 → 25점
elif days_old > 90: base_score -= 15  # 10점 → 15점
issue_penalty = min(len(issues) * 10, 40)  # 5점 → 10점

# 추가 감점
if days_old > 730:  # 2년 이상
    total_score *= 0.5  # 절반 감점
```

**주요 변경점**:
- 경과 일수 감점 증가
- 이슈당 감점 5점 → 10점
- 2년 이상: 50% 추가 감점

---

### 6. **유용성 (Usability)** - `usability.py`

#### 변경 전:
```python
if total_rows < 10: base_score -= 30
elif total_rows < 100: base_score -= 15
elif total_rows < 1000: base_score -= 5

if col_usability < 50: base_score -= 20
elif col_usability < 75: base_score -= 10

issue_penalty = min(issue_count * 3, 20)
```

#### 변경 후:
```python
if total_rows < 10: base_score -= 40  # 30점 → 40점
elif total_rows < 100: base_score -= 20  # 15점 → 20점
elif total_rows < 1000: base_score -= 10  # 5점 → 10점

if col_usability < 50: base_score -= 30  # 20점 → 30점
elif col_usability < 75: base_score -= 15  # 10점 → 15점

issue_penalty = min(issue_count * 8, 40)  # 3점 → 8점

# 추가 감점
if total_rows < 10:
    total_score *= 0.6  # 40% 추가 감점
```

**주요 변경점**:
- 데이터 양 감점 증가
- 컬럼 유용성 감점 증가
- 이슈당 감점 3점 → 8점
- 데이터 <10건: 40% 추가 감점

---

## 📊 기대 효과

### 예시: sample_error_heavy.csv

**데이터 오류**:
- 50% NULL 값 (order_count)
- 30% 중복 레코드 (user_id: 1001 3회)
- 음수 값 (-5, -10)
- 미래 연도 (2025-2030)
- 과거 연도 (1899)
- 10건의 작은 데이터셋

**변경 전 예상 점수**: ~95점 ❌

**변경 후 예상 점수**: ~30-50점 ✅

---

## 🎯 점수 구간별 의미

| 점수 구간 | 등급 | 의미 |
|----------|------|------|
| 90-100 | 🟢 우수 | 품질 이슈가 거의 없음 |
| 70-89 | 🟡 양호 | 경미한 이슈가 일부 존재 |
| 50-69 | 🟠 보통 | 중간 수준의 품질 문제 |
| 0-49 | 🔴 미흡 | 심각한 품질 문제 존재 |

---

## 📈 개선 원칙

### 1. **더 엄격한 기준**
- 이슈당 감점 대폭 증가 (5점 → 8-10점)
- 기본 가중치 감소 (70% → 60%)

### 2. **다단계 감점**
- 기본 감점 + 추가 감점 조합
- 심각도에 따른 곱셈 감점 (×0.5, ×0.7)

### 3. **실질적 반영**
- NULL 50% → 완전성 50% 미만 → 최종 점수 절반
- 중복 30% → 일관성 대폭 감소
- 음수/미래 날짜 → 정확성 이슈 증가

### 4. **누적 효과**
- 여러 모듈에서 동시에 감점
- 전체 품질 점수는 평균이므로 한 모듈이 나쁘면 전체 하락

---

## 🔍 테스트 케이스

### sample_error_heavy.csv 예상 진단 결과:

**완전성 (Completeness)**: ~40점
- NULL 50% → completeness_rate = 50%
- base_score = 50 × 0.6 = 30
- issue_penalty = 이슈 5개 × 8 = 40점 (최대 40)
- issue_score = 40 - 40 = 0
- total = 30 + 0 = 30
- completeness_rate < 50 → 30 × 0.5 = **15점**

**일관성 (Consistency)**: ~35점
- 중복률 30% → duplicate_score = max(0, 100 - 30×5) × 0.5 = 0
- issue_penalty = 이슈 3개 × 10 = 30
- issue_score = 50 - 30 = 20
- total = 0 + 20 = 20
- duplicate_rate ≥ 20 → 20 × 0.5 = **10점**

**정확성 (Accuracy)**: ~40점
- 음수, 미래/과거 연도 오류
- 이슈 3-4개 발생
- accuracy_rate ~90% (전체 40개 값 중 4개 오류)
- base_score = 90 × 0.6 = 54
- issue_penalty = 4 × 10 = 40
- issue_score = 40 - 40 = 0
- total = 54 + 0 = 54
- accuracy_rate ≥ 70 → **54점**

**유용성 (Usability)**: ~30점
- 데이터 10건
- base_score = 100 - 40 = 60
- issue_penalty = 2 × 8 = 16
- total = 60 - 16 = 44
- total_rows < 10 → 44 × 0.6 = **26점**

**전체 평균**: (15 + 10 + 54 + 26) / 4 = **26.25점** ✅

---

## ✅ 결론

점수 계산 알고리즘을 **대폭 강화**하여:

1. ✅ 실제 품질 문제를 정확히 반영
2. ✅ 심각한 이슈에 강력한 페널티
3. ✅ 다층적 감점 시스템 (기본 + 추가)
4. ✅ 오류가 많은 데이터는 낮은 점수 (30-50점대)
5. ✅ 우수한 데이터는 높은 점수 (90-100점)

**이제 데이터 품질 진단 툴이 실제 데이터 품질을 정확하게 평가합니다!** 🎉
